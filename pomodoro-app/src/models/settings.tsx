export interface AppSettings {
  rounds: number;
  workTime: string;
  breakTime: string;
  longBreakTime: string;
  status: number;
  persent: number;
  selectedBreakTime: string;
  selectedRounds: number;
}
